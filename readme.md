# FastAPI

# Getting Start

```sh
$ pipenv install

$ pipenv run uvicorn src.main:app --reload
```

# WSL
```sh
# for intellisense python use
@ext:ms-python.vscode-pylance

$ pipenv --venv
# and add extra path
```
